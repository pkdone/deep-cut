import { artistImageCacheRecordSchema } from '../../../domain/schemas/artist-image-cache-record.js';

/** MongoDB document shape for `artist_image_cache` (derived from domain schema). */
export const artistImageDocumentSchema = artistImageCacheRecordSchema;
