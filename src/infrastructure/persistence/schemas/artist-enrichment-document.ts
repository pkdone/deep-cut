import { artistInsightsRecordSchema } from '../../../domain/schemas/artist-insights-record.js';

/** MongoDB document shape for `artist_enrichment_cache` (derived from domain; single source of field rules). */
export const artistEnrichmentDocumentSchema = artistInsightsRecordSchema;
