import { artistEnrichmentCacheSchema } from '../../../domain/schemas/artist-enrichment.js';

/** MongoDB document shape for `artist_enrichment_cache` (derived from domain; single source of field rules). */
export const artistEnrichmentDocumentSchema = artistEnrichmentCacheSchema;
